// We might need 'use client' if SavingsCalculator uses client-side hooks
// import { SavingsCalculator } from '@/components/savings-calculator' // Adjust path if needed
import SavingsCalculator from '@/components/savings-calculator'

export default function SavingsPage() {
	return <SavingsCalculator />
}
